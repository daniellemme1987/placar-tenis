name: Testes Automatizados Node.js

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 🔄 Checkout do código
      uses: actions/checkout@v3

    - name: ⚙️ Configura Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: 📦 Instala dependências
      run: npm install

    - name: 🧪 Roda os testes
      run: npm test
